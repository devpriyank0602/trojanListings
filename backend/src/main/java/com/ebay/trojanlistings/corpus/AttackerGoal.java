package com.ebay.trojanlistings.corpus;

/** What the attacker wants the agent to do. Secondary reporting axis (FR-004). */
public enum AttackerGoal {
    SELF_PROMOTION,
    COMPETITOR_DISPARAGEMENT,
    DISCLOSURE_SUPPRESSION,
    INSTRUCTION_EXTRACTION
}
